# Copy to .env and fill in your values
# For Netlify: add these as environment variables in Site settings > Environment variables

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-supabase-anon-key

# Optional: for server-side payment processing (Edge Functions). Set in Supabase Dashboard → Project Settings → Edge Functions → Secrets.
# PayPal (booking flow): PAYPAL_CLIENT_ID, PAYPAL_CLIENT_SECRET. For production set PAYPAL_MODE=live (default is sandbox).
# STRIPE_SECRET_KEY=sk_live_...
