# Copy to .env and fill in your values
# For Netlify: add these as environment variables in Site settings > Environment variables

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-supabase-anon-key

# Main app domain for custom-domain detection. When a visitor lands on a host different from this (and not localhost), the app resolves tenant by custom_domain and shows the booking page at /.
# Example: your-app.netlify.app or iska-service-os.com. Leave empty to disable custom domain resolution.
VITE_APP_MAIN_DOMAIN=

# Optional: for server-side payment processing (Edge Functions). Set in Supabase Dashboard → Project Settings → Edge Functions → Secrets.
# PayPal (booking flow): PAYPAL_CLIENT_ID, PAYPAL_CLIENT_SECRET. For production set PAYPAL_MODE=live (default is sandbox).
# STRIPE_SECRET_KEY=sk_live_...

# Optional: for booking emails (confirmation, cancellation, reminder, no-show). Set in Supabase Edge Functions → Secrets.
# RESEND_API_KEY=re_...
# RESEND_FROM_EMAIL=bookings@yourdomain.com
